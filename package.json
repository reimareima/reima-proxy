{
  "name": "reima-proxy",
  "version": "1.0.0",
  "description": "Backend proxy for Reima — hides API keys, rate-limits requests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
